// This is a dummy file for "mermaid.min.js" to be bundled for JavaDoc rendering
// JavaDoc doesn't allow to specify URL for --add-script parameter's value as of Java 25 EA.
// The approach we take for workaround is to specify this file to create a placeholder in each HTML file, then
// replace the file name with mermaid.min.js's distribution URL.